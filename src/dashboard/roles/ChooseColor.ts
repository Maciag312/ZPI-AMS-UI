export const chooseColor = (col: string) => { 
    if(col === "1"){
        return "MediumSeaGreen"
    } else if(col === "2"){
        return "DeepSkyBlue"
    } else if(col === "3") {
        return "Crimson"
    } else if(col === "4") {
        return "GoldenRod"
    }
}